import styled from "styled-components";
import { Table } from "antd";

export const Employees = styled(Table)`
  && {
    border-radius: 2px;
    padding: 16px;
    background: white;
    width: 100%;
  }
`;
